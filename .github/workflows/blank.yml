name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: nokotech/actions
      uses: nokotech/actions@master
    - name: ls
      run: |
        ls -la 
